CREATE TABLE IF NOT EXISTS imooc_log(
   page_id VARCHAR(200),
   time VARCHAR(200),
   name VARCHAR(200),
   person VARCHAR(200),
   difficulty VARCHAR(200),
   duration VARCHAR(200),
   score VARCHAR(200)
)